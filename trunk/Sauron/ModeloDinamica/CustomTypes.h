#pragma once

namespace sauron
{
	// leitura de um odometro distancia
	typedef double distanceReading_t;
	// leitura de um odometro
	typedef double thetaReading_t;
	// posição global em X (cm), Y (cm) ou th (rad)
	typedef double pose_t;
}